{
  "name": "input",
  "version": "0.0.0",
  "description": "A input component via using bel and csjs-inject",
  "main": "src/input.js",
  "scripts": {
    "start": "budo demo/demo.js:bundle.js --force-default-index --live --open --ssl",
    "build": "browserify demo/demo.js -o bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "input",
    "bel",
    "csjs-inject"
  ],
  "author": "@fionataeyang",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^16.5.0",
    "budo": "^11.6.3"
  },
  "dependencies": {
    "bel": "^6.0.0",
    "csjs-inject": "^1.0.1"
  }
}
